{
  "name": "@cloudpress/frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev . --port 3000",
    "build": "echo 'No build step required — static assets served as-is'",
    "deploy": "wrangler pages deploy . --project-name=cloudpress-bridge --branch=main"
  },
  "devDependencies": {
    "wrangler": "^3.109.1"
  }
}
